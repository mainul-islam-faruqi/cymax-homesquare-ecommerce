import { Styles } from '@chakra-ui/theme-tools'

const styles: Styles = {
  global: (_) => ({
    body: {
      fontVariantLigatures: 'none', // disabling Font Ligatures globally
    },
  }),
}

export default styles
